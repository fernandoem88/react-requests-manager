// useAppState, // in one app there are many contexts (userContext, postContext)
// useContextState, // in one context there are many actions (addUser, deleteUser, abortAddUser)
// useRequestState, // in one action there are many processes (addUser with params 1, params 2, ...)

// createUseAppState
// createUseContextState

export default null;
